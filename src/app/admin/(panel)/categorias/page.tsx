import { AdminPlaceholderPage } from "@/components/admin/admin-placeholder-page";

export default function AdminCategoriesPage() {
  return (
    <AdminPlaceholderPage
      eyebrow="Categorías"
      title="Administración de categorías preparada"
      description="El modelo ya incluye slug único, orden de visualización, SEO, estados y restricciones para evitar borrar categorías con productos activos."
    />
  );
}
