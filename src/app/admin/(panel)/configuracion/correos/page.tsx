import { AdminPlaceholderPage } from "@/components/admin/admin-placeholder-page";

export default function AdminEmailSettingsPage() {
  return (
    <AdminPlaceholderPage
      eyebrow="Correos"
      title="Centro de correos pendiente de UI"
      description="Las plantillas HTML/texto y los logs de envío ya tienen espacio definido para integrarse con Resend desde servidor."
    />
  );
}
