import { AdminPlaceholderPage } from "@/components/admin/admin-placeholder-page";

export default function AdminStoreSettingsPage() {
  return (
    <AdminPlaceholderPage
      eyebrow="Tienda"
      title="Configuración comercial lista"
      description="Los datos reales del negocio, contacto, checkout, textos legales y pie de página se almacenarán en `store_settings`."
    />
  );
}
