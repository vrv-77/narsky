import { AdminPlaceholderPage } from "@/components/admin/admin-placeholder-page";

export default function AdminAppearanceSettingsPage() {
  return (
    <AdminPlaceholderPage
      eyebrow="Apariencia"
      title="Tema visual listo a nivel de datos"
      description="La base ya incluye colores, tipografías, radios, anchos y secciones de inicio en `theme_settings` para conectar una vista previa real."
    />
  );
}
